<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:oracle" x:Class="oracle.MainWindow"
        Title="ちら美" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions><StackPanel />
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="exec" HorizontalAlignment="Left" Width="75" Margin="5" Click="Exec_Click"/>
            <TextBox x:Name="sqlText" Text="SQL" Margin="5" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Width="300" Height="50" AcceptsReturn="True"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding X}" />

    </Grid>
</Window>
