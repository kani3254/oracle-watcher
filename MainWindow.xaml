<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:oracle" x:Class="oracle.MainWindow"
        Title="ちら美" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions><StackPanel />
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="exec" HorizontalAlignment="Left" Width="75" Margin="5" Click="Exec_Click"/>
            <TextBox x:Name="sqlText" Text="SQL" Margin="5" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Width="300" Height="50" AcceptsReturn="True"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <ComboBox x:Name="table" Width="Auto" Margin="5" ItemsSource="{Binding TableData}" DisplayMemberPath="TABLE_NAME" SelectedValuePath="TABLE_NAME">
                <ComboBox.DataContext>
                    <local:OracleDataSet SqlText="select owner || '.' || table_name as TABLE_NAME from all_tables" />
                </ComboBox.DataContext>
            </ComboBox>
            <TextBox Margin="5" Text="{Binding SelectedValue, ElementName=table}" />
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding TableData}" >
            <DataGrid.DataContext>
                <local:OracleDataSet SqlText="{Binding SelectedValue, ElementName=table}"/>
            </DataGrid.DataContext>
        </DataGrid>

    </Grid>
</Window>
